version: 1
name: test1

defaults:
  mode: RGB
  pipeline: encode
  decode_iterations: 8
  image: ../../tests/test-data/lena-colour.png
  encoder:
    max_block: 8
    min_block: 4
    topk: 64
    max_mse: 0.06
    entropy_thresh: 0.0
    use_quadtree: true
    quantized: true
    stride: 12