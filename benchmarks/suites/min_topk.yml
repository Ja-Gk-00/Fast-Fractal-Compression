version: 1
name: test1

defaults:
  mode: RGB
  pipeline: encode
  decode_iterations: 8
  image: ../../tests/test-data/lena-colour.png
  encoder:
    max_block: 16
    min_block: [2,4,8,16]
    topk: [8,16,24,32,64]
    entropy_thresh: 0.0
    use_quadtree: true
    quantized: true
    stride: 4